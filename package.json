{
  "name": "three-pages",
  "version": "1.0.0",
  "author": "Hariharan Selvaraj",
  "private": true,
  "description": "Any book. Three pages. Any language.",
  "scripts": {
    "dev": "cd apps/api && cargo run && cd apps/web && pnpm dev",
    "dev:web": "cd apps/web && pnpm dev",
    "dev:api": "cd apps/api && cargo run",
    "build": "cd apps/web && pnpm install && pnpm run build && cd ../.. && cp -r apps/web/dist .",
    "build:web": "cd apps/web && pnpm build",
    "build:api": "cd apps/api && cargo build --release",
    "vercel-build": "cd apps/web && pnpm install && pnpm run build",
    "install:web": "cd apps/web && pnpm install",
    "lint:web": "cd apps/web && pnpm lint",
    "test:api": "cd apps/api && cargo test",
    "clean": "turbo run clean && rm -rf node_modules && rm -rf dist",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "start": "cd apps/web && pnpm preview",
    "deploy:web": "vercel --prod"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
